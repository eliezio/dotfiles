services:
  dotfiles2:
    build: .
    environment:
      USER: manjaro
    volumes:
      - type: bind
        source: $PWD/../..
        target: /home/manjaro/.local/share/chezmoi
      - type: bind
        source: $HOME/.config/sops/age/keys.txt
        target: /home/manjaro/.config/sops/age/keys.txt
        read_only: true
    command: [ "sleep", "infinity" ]
    working_dir: /home/manjaro/.local/share/chezmoi
